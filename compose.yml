# 本地开发使用。
# 只启动 Redis，PostgreSQL 直接使用本机安装的实例。
# 启动命令：docker compose up -d
name: pinjie-fullstack-blog-local
services:
  redis:
    image: redis:8.10.0-alpine
    ports:
      - "127.0.0.1:6381:6379"
    volumes:
      - redis_data:/data
    restart: unless-stopped

volumes:
  redis_data:
